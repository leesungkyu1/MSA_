spring:
  application:
    #    컨피그 서버에서 사용할 이름
    name: licensing-service
  profiles:
    active: dev
  cloud:
    config:
      #      컨피그 서버 위치
      #  도커 이미지로 배포할땐 컨테이너명으로 변경
      uri: http://configserver:8071
#      uri: http://localhost:8040
